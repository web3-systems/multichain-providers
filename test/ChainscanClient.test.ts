// import { ChainscanClient } from '../src';

const apikey = process.env.ETHERSCAN_API_KEY || '';

describe('ChainscanClient', () => {
  describe('success', () => {
    it('should succeed to create a ChainscanClient for Etherscan Mainnet', () => {
      // let client = new ChainscanClient(1, apikey);
      // expect(client.clients[1]).toBeTruthy();
    });
  });
});
